import AboutClient from "@/components/about/AboutClient";

export const metadata = {
  title: "About Us | Mother Era",
  description: "Empowering mothers globally with expert, culturally relevant, and compassionate healthcare guidance.",
};

export default function AboutPage() {
  return <AboutClient />;
}
